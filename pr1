# Задача 1: Создаем список и выводим его
numbers = [1, 2, 3, 4, 5]
print("Задача 1 - Список:", numbers)

# Задача 2: Находим сумму чисел в списке
total = sum(numbers)
print("Задача 2 - Сумма:", total)

# Задача 3: Находим максимальное число в списке
max_num = max(numbers)
print("Задача 3 - Максимум:", max_num)

# Задача 4: Умножаем каждый элемент списка на 2
doubled = [x * 2 for x in numbers]
print("Задача 4 - Удвоенные числа:", doubled)

# Задача 5: Оставляем только чётные числа
even_numbers = [x for x in numbers if x % 2 == 0]
print("Задача 5 - Чётные числа:", even_numbers)

# Задача 6: Считаем количество элементов в списке
count = len(numbers)
print("Задача 6 - Количество элементов:", count)
